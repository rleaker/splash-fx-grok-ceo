services:
  - type: web
    name: splash-fx-grok-ceo
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn grok-ceo:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GROK_API_KEY
        sync: false
